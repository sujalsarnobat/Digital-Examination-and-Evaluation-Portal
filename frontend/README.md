# Frontend - Digital Examination and Evaluation Portal

React + Vite frontend for the portal.

## Run

1. Install dependencies:

```bash
npm install
```

2. Start dev server:

```bash
npm run dev
```

The app proxies `/api` requests to `http://localhost:8080`.
